// Create the addNumbers function here.

var two = 2;
var three = 3;
function addnumbers(x, y)
{    
    var five = x + y;
    
    alert(five);
}

addnumbers(two, three);

// Create the stringConcat function here.
function stringConcat()
{
    var newString = 'new' + ' ' + 'string';

    // alert(newString);
}